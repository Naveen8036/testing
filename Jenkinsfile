node {
    
    stage('Build') {
    
            if ( a -ge b) {
                sh "a is greater than b "
            } else {
                sh "a is less than b"
            }
        
    }
    
}
